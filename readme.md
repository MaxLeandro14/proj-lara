cp .env.example .env (caso não tenha o arquivo .env) </br>
php artisan key:generate</br>
php artisan serve</br>

Para rodar as migrations, não esquecer de configurar o banco de dados,</br>
em seguida:</br>
php artisan migrate</br>

